services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
  frontend:
    build:
      context: ./nm-frontend
      dockerfile: Client.Dockerfile
    ports:
      - "4000:4000"
